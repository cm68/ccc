#include "ccc.h"

struct expr *
expr(char priority, struct stmt *parent)
{
    struct expr *e;

    return e;
}

void
freeexpr(struct expr *e)
{
    free(e);
}

/*
 * vim: tabstop=4 shiftwidth=4 expandtab:
 */
